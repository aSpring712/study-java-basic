package class1;

/**
 * Student
 *
 * 클래스 도입 - Student 클래스
 *
 * @author Kwon Bomi / GitHub: aSpring712
 * @since 2025-04-02
 * @version 1.0
 */
public class Student { // class 키워드를 사용해 학생 클래스(Student)를 정의

	// 학생 클래스는 내부에 이름(name), 나이(age), 성적(grade) 변수를 가진다. -> 클래스에 정의한 변수들을 멤버 변수, 또는 필드라고 한다.
	String name;
	int age;
	int grade;
}